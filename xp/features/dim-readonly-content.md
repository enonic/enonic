# Dim content where the user has only read access (Content Studio)

As a user, I want content items that I have only read-only access to to be visually highlighted in the Content Grid.

## Estimate

2 hours

## Requirements


** To be concluded after response from TSI **

* Dim content items with read-only access in the grid
* Show a tooltip that says "Read-only" on hover of a content item with read-only access

## Dependencies

None

## Acceptance Criteria

* All requirements are met
* User test is satisfactory
